"""AMP command helpers.

The first AMP flat task uses IsaacLab's velocity command through the locomotion MDP re-export.
This module is kept so AMP command-specific behavior has a stable home when needed.
"""
